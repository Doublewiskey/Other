# Создаем функцию калькулятора
def calculate(expression):
    # В качестве параметра передаем математическое выражение
    try:
        result = eval(expression)
        # Возвратим результат выражения
        return result
    except:
        # Если возникла ошибка, вернем сообщение об ошибке
        return "Ошибка в выражении"


# Пример использования калькулятора
expression = input("Введите математическое выражение: ")
# Вызываем функцию калькулятора и выводим результат
print("Результат = ", calculate(expression))
